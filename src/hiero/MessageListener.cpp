#include "MessageListener.h"
#include "TopicMessageQuery.h"

namespace hiero {
	MessageListener::MessageListener(const AccountId& topicId)
		: mTopicId(topicId)
	{

	}

	MessageListener::~MessageListener()
	{

	}

	void MessageListener::start()
	{

	}
}